<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hunji.system.mapper.SysUserMapper">
    <select id="getAllUsersMaster" resultType="com.hunji.system.domain.SysUser">
        select * from sys_user
    </select>
    <select id="getAllUsersSlave" resultType="com.hunji.system.domain.SysUser">
        select * from sys_user
    </select>
</mapper>